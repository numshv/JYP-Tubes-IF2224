program if_test;
variabel
    x, y, result: integer;
    average: real;
mulai
    x := 10;
    y := 20;
    result := x + y;
    average := result / 2;
    jika result > 25 maka
        writeln('Damn bro that is huge');
    selain-itu
        writeln('Damn bro that is kinda small ..');
selesai.
