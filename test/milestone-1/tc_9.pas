program EXAMPLE1;
variabel
    x, y, sum: real;
mulai
    x := 5;
    y := 10;
    sum := x + y;
    yeay := 'true';

    writeln('Sum = ', sum);

    jika (sum > 10) dan (sum < 1000) maka
        writeln('Sum > 10');
    selain-itu
        writeln('Sum <= 10');

    writeln('Done!');
selesai.
